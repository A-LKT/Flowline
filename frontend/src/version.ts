// App version, injected at build time from frontend/package.json (see vite.config.ts).
// Bump the version in package.json — everything that shows it reads from here.
export const APP_VERSION: string = __APP_VERSION__;
